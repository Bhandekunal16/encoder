export type LinksConfig = {
  back: string;
  wordDecoder: string;
};

export type ButtonsConfig = {
  convert: string;
  revert: string;
};

export type ErrorConfig = {
  somethingWentWrong: string;
};
